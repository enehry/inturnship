import 'package:flutter/material.dart';

class ViewLogEntry extends StatelessWidget {
  const ViewLogEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
